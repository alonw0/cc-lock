{
  "name": "@cc-lock/daemon",
  "version": "0.1.0",
  "description": "Background daemon for cc-lock — manages lock state, scheduling, usage tracking, and shim management",
  "type": "module",
  "license": "MIT",
  "homepage": "https://github.com/alonw0/cc-lock#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/alonw0/cc-lock.git",
    "directory": "packages/daemon"
  },
  "keywords": [
    "claude",
    "claude-code",
    "cc-lock"
  ],
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "prepack": "cp ../../LICENSE .",
    "build": "tsup",
    "dev": "tsup --watch --onSuccess 'node dist/index.js'",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cc-lock/check-lock": "workspace:*",
    "@cc-lock/core": "workspace:*",
    "better-sqlite3": "^11.0.0",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
